#include <iostream>
using namespace std;

/**
 * Return the CALICOncatenation of A.
 *
 * A: a string of representing a single word
 */

int solve(string A) {
    // YOUR CODE HERE
    return -1;
}

int main() {
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) {
        string A;
        cin >> A;
        cout << solve(A) << '\n';
    }
}
