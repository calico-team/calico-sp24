#include <iostream>
using namespace std;

/**
 * Inputs:
 * S: The string from which to find the substrings.
 * P: The string to make by concatenating substrings.
 *
 * Print a single line containing four space-separated integers s1 l1 s2 l2 where:
 * - s1 denotes the starting position of the first substring
 * - l1 denotes the length of the first substring
 * - s2 denotes the starting position of the second substring
 * - l2 denotes the length of the second substring
 * such that concatenating the first substring and the second substring yields P.
 * When giving positions, the position of the first character is 0 (zero-indexed),
 * the position of the second character is 1, the position of the third character
 * is 2, and so on.
 */
void solve(string S, string P) {
    return;
}

int main() {
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) {
        string S, P;
        cin >> S >> P;
        solve(S, P);
    }
}
