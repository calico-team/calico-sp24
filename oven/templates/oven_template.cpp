#include <iostream>

using namespace std;

/**
 * Return the new temperature after performing action A with a current
 * temperature of D.
 * 
 * D: the current temperature
 * A: the action to perform ("INCREMENT", "DECREMENT", or "MAINTAIN")
 */
int solve(int D, string A) {
    // YOUR CODE HERE
    return 0;
}

int main() {
    int D;
    cin >> D;
    string A;
    cin >> A;
    cout << solve(D, A) << '\n';
}
